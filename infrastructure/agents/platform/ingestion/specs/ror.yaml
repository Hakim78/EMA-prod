source_id: ror
source_num: 13
layer: 2
couche: IDs internationaux
name: ROR — Research Org Registry
description: 'Source #13 ARCHITECTURE_DATA_V2. Bronze ingestion raw — traité ensuite
  par silver/gold/mart.'
endpoint: https://api.ror.org/organizations
auth: none
pattern: rest_json_public
refresh_trigger: interval_hours=720
sla_minutes: 43260
license: Public / Etalab 2.0 par défaut
bronze_table: bronze.ror_orgs_raw
bronze_fields:
- name: ror_id
  type: VARCHAR(64)
  primary_key: true
- name: name
  type: VARCHAR(512)
  primary_key: false
- name: country
  type: VARCHAR(8)
  primary_key: false
- name: types
  type: TEXT[]
  primary_key: false
conflict_strategy: ON CONFLICT (ror_id) DO UPDATE SET payload = EXCLUDED.payload,
  ingested_at = now()
