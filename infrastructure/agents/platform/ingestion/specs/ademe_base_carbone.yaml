source_id: ademe_base_carbone
source_num: 85
layer: 13
couche: ESG
name: ADEME Base Carbone — facteurs émission
description: 'Source #85 ARCHITECTURE_DATA_V2. Bronze ingestion raw — traité ensuite
  par silver/gold/mart.'
endpoint: https://data.ademe.fr/api/records/1.0/search
auth: none
pattern: rest_json_public
refresh_trigger: interval_hours=8760
sla_minutes: 525660
license: Public / Etalab 2.0 par défaut
bronze_table: bronze.ademe_facteurs_raw
bronze_fields:
- name: facteur_id
  type: VARCHAR(64)
  primary_key: true
- name: nom_base
  type: VARCHAR(512)
  primary_key: false
- name: valeur
  type: NUMERIC
  primary_key: false
- name: unite
  type: VARCHAR(64)
  primary_key: false
- name: source
  type: VARCHAR(255)
  primary_key: false
conflict_strategy: ON CONFLICT (facteur_id) DO UPDATE SET payload = EXCLUDED.payload,
  ingested_at = now()
